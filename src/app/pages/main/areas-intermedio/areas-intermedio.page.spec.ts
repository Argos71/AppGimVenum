import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AreasIntermedioPage } from './areas-intermedio.page';

describe('AreasIntermedioPage', () => {
  let component: AreasIntermedioPage;
  let fixture: ComponentFixture<AreasIntermedioPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AreasIntermedioPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
